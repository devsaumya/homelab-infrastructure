apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cilium-system

resources:
  - namespace.yaml
  # Cilium is typically installed via Helm chart
  # Add Helm chart reference or manifests here
  # Example: helm install cilium cilium/cilium --namespace cilium-system

commonLabels:
  app: cilium

