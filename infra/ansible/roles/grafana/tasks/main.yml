---
# Grafana installation preparation

- name: Create Grafana directories
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - /opt/docker/monitoring/grafana
    - /data/grafana

- name: Set Grafana admin password
  set_fact:
    grafana_admin_password: "{{ vault_grafana_admin_password | default('admin') }}"
