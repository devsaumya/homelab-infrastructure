# Kyverno Policy Engine
# Note: Kyverno is typically installed via Helm chart
# This is a placeholder - use Helm for production deployment
#
# Installation command:
# helm repo add kyverno https://kyverno.github.io/kyverno/
# helm repo update
# helm install kyverno kyverno/kyverno \
#   --namespace kyverno \
#   --set replicaCount=1

apiVersion: v1
kind: ConfigMap
metadata:
  name: kyverno-config
  namespace: kyverno
  annotations:
    argocd.argoproj.io/sync-wave: "5"
data:
  # Kyverno configuration placeholder
  # Actual config should come from Helm values
